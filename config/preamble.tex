% pacotes
\usepackage{quoting, setspace, caption, float}
\usepackage[brazil]{babel}

% ambiente citação
\newenvironment{citacao}
    {\begin{quoting}[rightmargin=0cm,leftmargin=4cm]
    \begin{singlespace}
    \footnotesize
    }
    {\end{singlespace}
    \end{quoting}
}

% tamanho da legenda
\captionsetup[figure]{font=scriptsize}

% posição das figuras
\floatplacement{figure}{H}
